import { ComingSoon } from "@/components/ComingSoon";

export default function LinksPage() {
  return (
    <ComingSoon
      title="Link & Affiliate Manager"
      description="UTM-tagged short links, the /go/{slug} redirector, click tracking and affiliation compliance checks land in Phase 10. The AffiliateLink and ClickEvent models already exist."
    />
  );
}
