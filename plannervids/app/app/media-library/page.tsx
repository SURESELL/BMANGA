import { ComingSoon } from "@/components/ComingSoon";

export default function MediaLibraryPage() {
  return (
    <ComingSoon
      title="Media Library"
      description="Centralized media (image/video/audio/logo/document) backed by S3-compatible object storage (Cloudflare R2) lands in Phase 5. The StorageProvider abstraction and MediaAsset model already exist."
    />
  );
}
