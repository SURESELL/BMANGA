import { ComingSoon } from "@/components/ComingSoon";

export default function CampaignsPage() {
  return (
    <ComingSoon
      title="Campaigns"
      description="Campaign management (linking content, products and affiliate links to a time-boxed initiative) lands alongside the Content Studio in Phase 3. The Campaign model already exists in the schema."
    />
  );
}
