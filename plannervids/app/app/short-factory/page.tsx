import { ComingSoon } from "@/components/ComingSoon";

export default function ShortFactoryPage() {
  return (
    <ComingSoon
      title="Short Factory"
      description="Template-based 9:16 video pipeline (script → storyboard → TTS voice-over → subtitles → FFmpeg/Remotion render → preview) lands in Phase 6. Low-cost by default — no generative video AI unless explicitly enabled."
    />
  );
}
