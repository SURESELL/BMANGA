import { ComingSoon } from "@/components/ComingSoon";

export default function ContentStudioPage() {
  return (
    <ComingSoon
      title="AI Content Studio"
      description="Content Masters, per-platform variants and AI-assisted generation (idea, hook, post, caption, hashtags, CTA) land in Phase 3. Fully usable without an AI key configured — manual authoring will always work."
    />
  );
}
