import { ComingSoon } from "@/components/ComingSoon";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      description="Normalized analytics collection per platform lands in Phase 9, after connectors exist to fetch real data from. Metrics a platform doesn't report will always show as unavailable, never as 0."
    />
  );
}
