import { ComingSoon } from "@/components/ComingSoon";

export default function SocialAccountsPage() {
  return (
    <ComingSoon
      title="Social Accounts"
      description="OAuth connection flows for LinkedIn, Facebook, Instagram, TikTok and YouTube land one platform at a time in Phase 7, each verified against current official API docs before being wired up. Nothing here will ever fake a 'connected' state."
    />
  );
}
