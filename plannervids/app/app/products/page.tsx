import { ComingSoon } from "@/components/ComingSoon";

export default function ProductsPage() {
  return (
    <ComingSoon
      title="Product Library"
      description="Product CRUD (ebook, KDP, training, SaaS, affiliate, ...) with brand scoping lands in Phase 3. The Product model already exists in the schema."
    />
  );
}
