import { ComingSoon } from "@/components/ComingSoon";

export default function ApprovalsPage() {
  return (
    <ComingSoon
      title="Approvals"
      description="Approve / reject / regenerate, bulk-select, Approve Week and Approve Month land with the Calendar in Phase 4. The workflow status machine (DRAFT → ... → PUBLISHED) already exists in the schema."
    />
  );
}
